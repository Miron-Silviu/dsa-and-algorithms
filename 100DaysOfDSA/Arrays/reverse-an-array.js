'use strict';

/* Write a function that takes and array and return a new array with the elements in reverse order


*/
const reverseArray = function (arr) {
  return arr.toReversed();
};
console.log(reverseArray([1, 2, 3, 4]));
