'use strict';
/**
 * Create and Manipulate Arrays
 *
 * Task:
 * Implement a function `createOddNumbersArray(n)` that takes a positive integer `n`
 * and returns an array containing the first `n` odd numbers.
 *
 * Example:
 * createOddNumbersArray(5) // returns [1, 3, 5, 7, 9]
 *
 */

// function createOddNumbersArray(n) {
//   const arr = [];
//   for (let i = 0; i < n; i++) {
//     arr.push(2 * i+1);
//   }
//   return arr;
// }
// console.log(createOddNumbersArray(20));
